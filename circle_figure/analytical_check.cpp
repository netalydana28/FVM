#include <bits/stdc++.h>  
using namespace std;

int n = 41, m = 81; 
double newP[n][m], oldP[n][m], e = 0.0001, max;
dx = 0.25/(double(n) - 1);
dy = 0.5/(double(m) - 1); 

vector<double> normal(double x1, double y1, double x2, double y2){
    vector<double> ans; 
    ans.push_back(y1 - y2);
    ans.push_back(x2 - x1);
    double len = pow(pow(ans[0], 2) + pow(ans[1], 2), 0.5);
}



int main(){
    


    return 0; 
}